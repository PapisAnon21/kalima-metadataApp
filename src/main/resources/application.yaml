server:
  port: 8081

spring:
  application:
    name: metadataApp
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/kalima
    username: papis
    password: papis
  jpa:
    hibernate:
      ddl-auto: update
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect

api:
  chatbot:
    base:
      url: 'http://localhost:8000'